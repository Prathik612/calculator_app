import 'package:flutter/material.dart';


class ButtonColor {
  /// colors for buttons
  static var numberButton = Colors.grey[400];
  static var operatorButton = Colors.grey[700];
  static var acButton = Colors.red[800];
  static var textColor = Colors.black;
}

class AppTheme{
  /// colors for the background and display
  static var bkGround = Colors.black;
  static var displayColor = Colors.greenAccent[400];
}